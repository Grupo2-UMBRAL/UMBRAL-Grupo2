# UMBRAL account console theme. In Keycloak 25 the account console is a compiled
# React SPA (keycloak.v3 / PatternFly 5), so branding is limited to tinting the
# PatternFly design tokens via CSS variables — see umbral.css.
parent=keycloak.v3
styles=css/umbral.css
