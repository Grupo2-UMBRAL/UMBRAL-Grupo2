services:
  postgres:
    image: postgres:16-alpine
    container_name: umbral-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./infra/postgres/init:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  rabbitmq:
    image: rabbitmq:3.13-management-alpine
    container_name: umbral-rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_DEFAULT_USER}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_DEFAULT_PASS}
    ports:
      - "${RABBITMQ_AMQP_PORT}:5672"
      - "${RABBITMQ_MANAGEMENT_PORT}:15672"
    volumes:
      - rabbitmq-data:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "check_port_connectivity"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s

  keycloak:
    image: quay.io/keycloak/keycloak:25.0
    container_name: umbral-keycloak
    command:
      - start-dev
      - --import-realm
      - --proxy=edge
      - --http-relative-path=/auth
      - --hostname=http://localhost:7500/auth
      - --hostname-backchannel-dynamic=true
    environment:
      KEYCLOAK_ADMIN: ${KEYCLOAK_ADMIN}
      KEYCLOAK_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD}
    ports:
      - "${KEYCLOAK_PORT}:8080"
    volumes:
      - ./infra/keycloak/import:/opt/keycloak/data/import
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "exec 3<>/dev/tcp/127.0.0.1/8080"]
      interval: 15s
      timeout: 5s
      retries: 10

  identity-access-service:
    build:
      context: .
      dockerfile: infra/keycloak/docker/identity-access.Dockerfile
    container_name: umbral-identity-access-service
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080
      Auth__Authority: ${AUTH_AUTHORITY}
      Auth__RequireHttpsMetadata: ${AUTH_REQUIRE_HTTPS_METADATA}
      Auth__Audience: umbral-identity-access-api
      IdentityAccess__Keycloak__BaseUrl: ${IDENTITY_ACCESS_KEYCLOAK_BASE_URL}
      IdentityAccess__Keycloak__Realm: ${IDENTITY_ACCESS_KEYCLOAK_REALM}
      IdentityAccess__Keycloak__AdminRealm: ${IDENTITY_ACCESS_KEYCLOAK_ADMIN_REALM}
      IdentityAccess__Keycloak__AdminClientId: ${IDENTITY_ACCESS_KEYCLOAK_ADMIN_CLIENT_ID}
      IdentityAccess__Keycloak__AdminUsername: ${IDENTITY_ACCESS_KEYCLOAK_ADMIN_USERNAME}
      IdentityAccess__Keycloak__AdminPassword: ${IDENTITY_ACCESS_KEYCLOAK_ADMIN_PASSWORD}
    ports:
      - "${IDENTITY_ACCESS_PORT}:8080"
    depends_on:
      keycloak:
        condition: service_started

  mission-design-service:
    build:
      context: .
      dockerfile: infra/keycloak/docker/mission-design.Dockerfile
    container_name: umbral-mission-design-service
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__Postgres: Host=postgres;Port=5432;Database=umbral;Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD};Search Path=mission_design
      Auth__Authority: ${AUTH_AUTHORITY}
      Auth__RequireHttpsMetadata: ${AUTH_REQUIRE_HTTPS_METADATA}
      Auth__Audience: umbral-mission-design-api
      Persistence__ApplyMigrationsOnStartup: "true"
    ports:
      - "${MISSION_DESIGN_PORT}:8080"
    depends_on:
      postgres:
        condition: service_healthy
      keycloak:
        condition: service_started

  session-operations-service:
    build:
      context: .
      dockerfile: infra/keycloak/docker/session-operations.Dockerfile
    container_name: umbral-session-operations-service
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__Postgres: Host=postgres;Port=5432;Database=umbral;Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD};Search Path=session_operations
      RabbitMQ__Host: rabbitmq
      RabbitMQ__Port: 5672
      RabbitMQ__User: ${RABBITMQ_DEFAULT_USER}
      RabbitMQ__Password: ${RABBITMQ_DEFAULT_PASS}
      ScoringAudit__BaseUrl: http://scoring-audit-service:8080/
      Auth__Authority: ${AUTH_AUTHORITY}
      Auth__RequireHttpsMetadata: ${AUTH_REQUIRE_HTTPS_METADATA}
      Auth__Audience: umbral-session-operations-api
      SignalR__Enabled: true
      Persistence__ApplyMigrationsOnStartup: "true"
    ports:
      - "${SESSION_OPERATIONS_PORT}:8080"
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      keycloak:
        condition: service_started

  scoring-audit-service:
    build:
      context: .
      dockerfile: infra/keycloak/docker/scoring-audit.Dockerfile
    container_name: umbral-scoring-audit-service
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__Postgres: Host=postgres;Port=5432;Database=umbral;Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD};Search Path=scoring_ops
      RabbitMQ__Host: rabbitmq
      RabbitMQ__Port: 5672
      RabbitMQ__User: ${RABBITMQ_DEFAULT_USER}
      RabbitMQ__Password: ${RABBITMQ_DEFAULT_PASS}
      Auth__Authority: ${AUTH_AUTHORITY}
      Auth__RequireHttpsMetadata: ${AUTH_REQUIRE_HTTPS_METADATA}
      Auth__Audience: umbral-scoring-audit-api
      Persistence__ApplyMigrationsOnStartup: "true"
    ports:
      - "${SCORING_AUDIT_PORT}:8080"
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      keycloak:
        condition: service_started

  edge-proxy:
    build:
      context: .
      dockerfile: infra/keycloak/docker/edge-proxy.Dockerfile
    container_name: umbral-edge-proxy
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080
      AllowedOrigins__0: http://localhost:5173
      AllowedOrigins__1: http://localhost:3000
      AllowedOrigins__2: http://localhost:${MOBILE_WEB_PORT}
      COVERAGE_REPORT_PATH: /app/coverage
      ReverseProxy__Clusters__identity-access__Destinations__primary__Address: http://identity-access-service:8080/
      ReverseProxy__Clusters__mission-design__Destinations__primary__Address: http://mission-design-service:8080/
      ReverseProxy__Clusters__session-operations__Destinations__primary__Address: http://session-operations-service:8080/
      ReverseProxy__Clusters__scoring-audit__Destinations__primary__Address: http://scoring-audit-service:8080/
      ReverseProxy__Clusters__keycloak__Destinations__primary__Address: http://keycloak:8080/
      ReverseProxy__Clusters__mobile__Destinations__primary__Address: http://mobile:19006/
      ReverseProxy__Clusters__web__Destinations__primary__Address: http://web:3000/
    ports:
      - "${EDGE_PROXY_PORT}:8080"
    volumes:
      - ./temp/validation/backend-coverage-report:/app/coverage
    depends_on:
      identity-access-service:
        condition: service_started
      mission-design-service:
        condition: service_started
      session-operations-service:
        condition: service_started
      scoring-audit-service:
        condition: service_started
      keycloak:
        condition: service_started

  web:
    build:
      context: ./src/apps/web
      dockerfile: Dockerfile
    container_name: umbral-web
    working_dir: /app
    command: ["npm", "run", "dev", "--", "--host"]
    environment:
      NODE_ENV: development
      PORT: ${WEB_PORT}
      WATCHPACK_POLLING: true
      KEYCLOAK_REALM: ${KEYCLOAK_REALM}
      KEYCLOAK_WEB_CLIENT_ID: ${KEYCLOAK_WEB_CLIENT_ID}
      EDGE_PROXY_INTERNAL_BASE_URL: ${EDGE_PROXY_INTERNAL_BASE_URL}
      KEYCLOAK_INTERNAL_BASE_URL: ${KEYCLOAK_INTERNAL_BASE_URL}
      NEXT_PUBLIC_EDGE_PROXY_BASE_URL: ${NEXT_PUBLIC_EDGE_PROXY_BASE_URL}
      NEXT_PUBLIC_KEYCLOAK_BASE_URL: ${NEXT_PUBLIC_KEYCLOAK_BASE_URL}
      NEXT_PUBLIC_SESSION_OPERATIONS_HUB_PATH: ${NEXT_PUBLIC_SESSION_OPERATIONS_HUB_PATH}
      NEXT_PUBLIC_SCORING_AUDIT_HUB_PATH: ${NEXT_PUBLIC_SCORING_AUDIT_HUB_PATH}
    ports:
      - "${WEB_PORT}:3000"
    volumes:
      - ./src/apps/web:/app
      - web-node-modules:/app/node_modules
    depends_on:
      edge-proxy:
        condition: service_started

  mobile:
    image: node:22-alpine
    container_name: umbral-mobile
    working_dir: /app
    command:
      - /bin/sh
      - -lc
      - >
        npm install &&
        npm run web:compose
    environment:
      CI: "1"
      NODE_ENV: development
      EXPO_NO_TELEMETRY: "1"
      EXPO_OFFLINE: "1"
      EXPO_NO_DEV_CLIENT: "1"
      EXPO_PORT: "${MOBILE_WEB_PORT}"
      EXPO_PUBLIC_EDGE_PROXY_BASE_URL: ${MOBILE_EDGE_PROXY_BASE_URL}
      EXPO_PUBLIC_KEYCLOAK_BASE_URL: ${MOBILE_KEYCLOAK_BASE_URL}
      EXPO_PUBLIC_KEYCLOAK_REALM: ${MOBILE_KEYCLOAK_REALM}
      EXPO_PUBLIC_KEYCLOAK_CLIENT_ID: ${MOBILE_KEYCLOAK_CLIENT_ID}
      EXPO_PUBLIC_SESSION_OPERATIONS_HUB_PATH: ${MOBILE_SESSION_OPERATIONS_HUB_PATH}
      EXPO_PUBLIC_SCORING_AUDIT_HUB_PATH: ${MOBILE_SCORING_AUDIT_HUB_PATH}
    ports:
      - "${MOBILE_WEB_PORT}:${MOBILE_WEB_PORT}"
    volumes:
      - ./src/apps/mobile:/app
      - mobile-node-modules:/app/node_modules
      - mobile-npm-cache:/root/.npm
    depends_on:
      edge-proxy:
        condition: service_started

volumes:
  postgres-data:
  rabbitmq-data:
  web-node-modules:
  mobile-node-modules:
  mobile-npm-cache:
